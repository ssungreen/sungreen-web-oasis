name = "sungreen-web-oasis"
compatibility_date = "2024-09-23"
pages_build_output_dir = "dist"

# All bindings (D1, R2) are configured in the Cloudflare Pages dashboard
# Settings > Functions > Bindings:
#   DB -> sungreen-db (D1)
#   BUCKET -> sungreen-cases (R2)

[vars]
ENVIRONMENT = "production"
